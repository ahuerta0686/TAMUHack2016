import { Meteor } from 'meteor/meteor';

import '../imports/api/users/users';
import '../imports/api/trips/trips';

import '../imports/startup/server';

Meteor.startup(() => {
  // code to run on server at startup
});
